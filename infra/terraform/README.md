# Terraform

Infrastructure roots live outside the API container and are mounted only into isolated runner jobs. Pin providers and runner images, keep state in an encrypted remote backend and use workload-scoped credentials. WC Hub stores plan metadata and digests, not plaintext state or secrets.

