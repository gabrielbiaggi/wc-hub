DROP TABLE IF EXISTS alerts, metrics_snapshots, audit_logs, file_index, storage_mounts, terraform_runs, terminal_sessions, projects, containers, virtual_machines, nodes, clusters, hosts, integrations, role_permissions, user_roles, permissions, roles, users CASCADE;
DROP TYPE IF EXISTS risk_level, action_scope, resource_status, integration_status;

